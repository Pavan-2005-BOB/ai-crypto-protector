// Simulated sentiment metrics (placeholder for real NLP or social media analysis)
module.exports = function getSentimentSignals() {
  return {
    sentimentScore: Math.floor(Math.random() * 100), // 0–100
    fudLevel: Math.floor(Math.random() * 100)        // 0–100
  };
};
